<aside class="right-side">
    <section class="content-header">
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active"><%= artist %></li>
        </ol>
    </section>
    <h1><%= artist %></h1>
    <table class="table">
        <thead>
            <tr>
                <th style="max-width: 20px;"></th>
                <th style="max-width: 5px;"></th>
                <th>Song</th>
                <th>Artist</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
            <tr data-ng-repeat="song in usersongs | startFrom:currentPage*pageSize | limitTo:pageSize">
                <td style="max-width: 20px;"><i style="margin-left: 20px;" class="fa fa-star" style="color:yellow;"></i> {{song.upvote}}</td>
                <td style="max-width: 5px;"><i style="margin-left: 10px;" class="fa fa-plus"></i></td>
                <td><a href="/viewsong/<%= results[i]._id %>">{{ results[i].title }}</a></td>
                <td><a href="/artist/<%= results[i].artist %>{{ results[i].artist }}</a></td>
                <td>{{ song.genre }}</td>
            </tr>
        </tbody>
        <button ng-disabled="currentPage == 0" ng-click="currentPage = currentPage-1">
            Previous
        </button>
            {{currentPage+1}}/{{numberOfPages()}}
        <button ng-disabled="currentPage >= usersongs.length/pageSize - 1"  ng-click="currentPage = currentPage+1">
            Next
        </button>
    </table>
</aside>